# Consumer ProGuard rules for :core.
# Empty for now -- shared code arrives in Phase 1. Any keep rules that the
# vendor jars (ysdk, iso8583, terminalLib, nanohttpd) require belong here so
# both apps inherit them rather than duplicating them in two proguard files.
